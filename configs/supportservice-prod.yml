server:
  port: 6001

spring:
  jpa:
    hibernate:
      ddl-auto: 
        update
  datasource:
    url: 
      jdbc:mysql://supportDB:3306/ms_support_tickets
    username: supportservice
    password: fiapteste

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://discover:8761/eureka